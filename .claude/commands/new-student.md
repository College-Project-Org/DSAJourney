Scaffold a new student directory for $ARGUMENTS (the student's name, lowercase).

Steps:
1. Create the directory: `mkdir -p <name>`
2. Create `<name>/sample.py` with a simple hello-world script
3. Remind the user to create a branch: `git checkout -b <name>_branch`

Do not add any other files. Keep it minimal.

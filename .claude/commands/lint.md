Run flake8 linting on the entire repository (mirrors CI).

```bash
source env/bin/activate && flake8 .
```

Report every violation with file path and line number. If there are no violations, confirm the repo is clean.
